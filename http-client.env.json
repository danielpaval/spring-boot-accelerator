{
  "localhost-user": {
    "TOKEN_URL": "http://localhost:8180/realms/dev/protocol/openid-connect/token",
    "CLIENT_ID": "private",
    "CLIENT_SCOPE": "openid profile email offline_access",
    "USER_NAME": "user@example.com",
    "API_URL": "http://localhost:8080",
    "Security": {
      "Auth": {
        "oauth2": {
          "Token URL": "{{TOKEN_URL}}",
          "Type": "OAuth2",
          "Grant Type": "Password",
          "Client ID": "{{CLIENT_ID}}",
          "Client Secret": "{{CLIENT_SECRET}}",
          "Username": "{{USER_NAME}}",
          "Password": "{{USER_PASSWORD}}"
        }
      }
    }
  },
  "localhost-admin": {
    "TOKEN_URL": "http://localhost:8180/realms/dev/protocol/openid-connect/token",
    "CLIENT_ID": "private",
    "CLIENT_SCOPE": "openid profile email offline_access",
    "USER_NAME": "admin@example.com",
    "API_URL": "http://localhost:8080",
    "Security": {
      "Auth": {
        "oauth2": {
          "Token URL": "{{TOKEN_URL}}",
          "Type": "OAuth2",
          "Grant Type": "Password",
          "Client ID": "{{CLIENT_ID}}",
          "Client Secret": "{{CLIENT_SECRET}}",
          "Username": "{{USER_NAME}}",
          "Password": "{{USER_PASSWORD}}"
        }
      }
    }
  },
  "azure-user": {
    "TOKEN_URL": "https://login.microsoftonline.com/{{TENANT_ID}}/oauth2/v2.0/token",
    "USER_NAME": "user@{{DOMAIN}}",
    "CLIENT_SCOPE": "openid profile email offline_access {{CLIENT_ID}}/.default",
    "Security": {
      "Auth": {
        "oauth2": {
          "Token URL": "{{TOKEN_URL}}",
          "Type": "OAuth2",
          "Grant Type": "Password",
          "Client ID": "{{CLIENT_ID}}",
          "Client Secret": "{{CLIENT_SECRET}}",
          "Username": "{{USER_NAME}}",
          "Password": "{{USER_PASSWORD}}",
          "Scope": "openid profile offline_access"
        }
      }
    }
  },
  "azure-admin": {
    "TOKEN_URL": "https://login.microsoftonline.com/{{TENANT_ID}}/oauth2/v2.0/token",
    "USER_NAME": "admin@{{DOMAIN}}",
    "CLIENT_SCOPE": "openid profile email offline_access",
    "Security": {
      "Auth": {
        "oauth2": {
          "Token URL": "{{TOKEN_URL}}",
          "Type": "OAuth2",
          "Grant Type": "Password",
          "Client ID": "{{CLIENT_ID}}",
          "Client Secret": "{{CLIENT_SECRET}}",
          "Username": "{{USER_NAME}}",
          "Password": "{{USER_PASSWORD}}",
          "Scope": "openid profile offline_access {{CLIENT_ID}}/.default"
        }
      }
    }
  }
}
